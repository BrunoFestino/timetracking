package com.example.timetracking.velocity.ui;

/**
 * Vaadin views for the velocity feature.
 *
 * <p>{@code VelocityView} is mapped to {@code @Route("velocity")} and compares the selected
 * delivered milestones: a bar chart of days-to-deliver plus a per-milestone and a per-person
 * breakdown of the effort behind each delivery.
 */
